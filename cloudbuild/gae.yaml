steps:
- name: 'ubuntu'
  args: ['bash', './create_env.sh']
  env:
  - 'PROJECT=$PROJECT_ID'

- name: 'gcr.io/cloud-builders/gcloud'
  args: ['app', 'deploy',
   '--no-promote',
   '--stop-previous-version',
   '--project', '${PROJECT_ID}',
   '--quiet']
